{
	"scripts": {
		"dev:tailwind": "pnpm dlx @tailwindcss/cli -i ./tailwind/style.css -o ./static/css/style.css --watch",
		"dev:fastapi": "echo 'http://localhost:8000' && uv run uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
		"dev": "pnpm run --parallel /dev:/"
	},
	"dependencies": {
		"@tailwindcss/cli": "^4.3.3",
		"tailwindcss": "^4.3.3"
	}
}